  $('#hud-area').textContent=`${state.area+1} / 5`;$('#hud-life').textContent=hearts(state.localLives);frame.dataset.nightmare=state.difficulty==='nightmare'?'1':'0';const partner=[...state.players.values()].find(p=>p.id!==state.playerId),mate=$('#mate-hud');mate.classList.toggle('hidden',state.mode!=='coop');$('#hud-mate').textContent=partner?`${hearts(partner.lives??3)}${partner.shield?' ◈':''}`:(state.connected?'WAIT':'---');if(itemStatus){const boost=Math.max(0,Math.ceil((state.boostUntil-performance.now())/1000));itemStatus.textContent=`${state.shield?'◈ SHIELD':'NO SHIELD'}${boost>0?` / ⚡ ${boost}s`:''}`;}
}
function updateGoalHud(){if(!state.goal)return;const dx=state.goal.x-local.x,dz=state.goal.z-local.z,dist=Math.hypot(dx,dz);$('#hud-goal').textContent=`${Math.round(dist)}m`;const targetYaw=Math.atan2(dx,-dz),relative=normAngle(targetYaw-local.yaw);if(goalArrow){goalArrow.style.transform=`rotate(${relative}rad)`;goalArrow.classList.toggle('near',dist<60);}if(goalDirection)goalDirection.textContent=`GOAL ${Math.round(dist)}m`;}
function updateTimer(){const base=state.startedAt||performance.now();$('#hud-time').textContent=fmt((performance.now()-base)/1000);}
function updateItemsVisual(now){if(!itemGroup)return;const t=now*.001;for(const g of itemGroup.children){if(!g.visible)continue;g.rotation.y=t*1.25+g.position.z*.01;g.position.y=1.05+Math.sin(t*2.3+g.position.x)*.12;}}
function drawMinimap(){if(!minimapCtx||!state.maze)return;const ctx=minimapCtx,w=minimapCanvas.width,h=minimapCanvas.height,m=state.maze,s=currentStage();ctx.clearRect(0,0,w,h);ctx.fillStyle='rgba(4,7,7,.88)';ctx.fillRect(0,0,w,h);const pad=10,scale=Math.min((w-pad*2)/s.width,(h-pad*2)/(s.length+10)),px=(x)=>w/2+x*scale,pz=(z)=>pad+(22-z)*scale;ctx.strokeStyle='rgba(220,230,225,.33)';ctx.lineWidth=1.2;for(const wall of state.mazeWalls){ctx.beginPath();ctx.moveTo(px(wall.x1),pz(wall.z1));ctx.lineTo(px(wall.x2),pz(wall.z2));ctx.stroke();}for(const item of state.items.values()){if(item.taken)continue;ctx.fillStyle=item.type==='medkit'?'#45ff82':item.type==='adrenaline'?'#ffbb44':'#61dfff';ctx.fillRect(px(item.x)-2,pz(item.z)-2,4,4);}let shown=0;for(const e of state.enemies.values()){if(e.dead||e.boss)continue;if(shown++>45)break;ctx.fillStyle=e.type==='runner'?'#ff7a37':'#d9535f';ctx.fillRect(px(e.x)-1.2,pz(e.z)-1.2,2.4,2.4);}const b=state.enemies.get('boss');if(b&&!b.dead){ctx.fillStyle='#ff2f55';ctx.beginPath();ctx.arc(px(b.x),pz(b.z),4,0,Math.PI*2);ctx.fill();}ctx.fillStyle='#54ff92';ctx.beginPath();ctx.arc(px(state.goal.x),pz(state.goal.z),4.5,0,Math.PI*2);ctx.fill();ctx.save();ctx.translate(px(local.x),pz(local.z));ctx.rotate(-local.yaw);ctx.fillStyle='#fff';ctx.beginPath();ctx.moveTo(0,-6);ctx.lineTo(4,5);ctx.lineTo(-4,5);ctx.closePath();ctx.fill();ctx.restore();for(const p of state.players.values()){if(p.id===state.playerId)continue;ctx.fillStyle='#ffd166';ctx.beginPath();ctx.arc(px(p.x),pz(p.z),3.2,0,Math.PI*2);ctx.fill();}}

function send(kind,payload={}){if(!state.channel||!state.connected)return Promise.resolve();return state.channel.send({type:'broadcast',event:'game',payload:{kind,from:state.playerId,...payload}}).catch(()=>{});}
function client(){return window.GameBackend?.client||null;}
async function connect(role,code){if(!client()){$('#coop-status').textContent='Supabase未接続';toast('リアルタイム接続を利用できません');return;}if(state.channel){try{await state.channel.unsubscribe();}catch{}}state.role=role;state.room=code;state.hostId=role==='host'?state.playerId:'';state.connected=false;state.partnerReady=false;const ch=client().channel(`outbreak23:${code}`,{config:{broadcast:{self:false,ack:false},presence:{key:state.playerId}}});state.channel=ch;ch.on('broadcast',{event:'game'},({payload})=>onNetwork(payload));ch.on('presence',{event:'sync'},()=>{const ids=Object.keys(ch.presenceState());state.partnerReady=ids.some(id=>id!==state.playerId);updateLobby();if(state.role==='guest'&&state.partnerReady)send('hello',{role:'guest'});});await new Promise(resolve=>ch.subscribe(async status=>{if(status==='SUBSCRIBED'){state.connected=true;await ch.track({id:state.playerId,role,at:Date.now()});send('hello',{role});updateLobby();resolve();}}));}
function updateLobby(){if(state.mode!=='coop')return;$('#coop-status').textContent=!state.connected?'接続中':state.partnerReady?'2人接続済み':'相手を待っています';$('#coop-detail').textContent=state.room?`ROOM ${state.room} / ${state.role.toUpperCase()}`:'Supabase Realtime / 2 players';startBtn.disabled=state.role!=='host'||!state.partnerReady;startBtn.textContent=state.role==='host'?(state.partnerReady?'協力ミッション開始':'パートナー待ち'):'ホストの開始を待つ';}
