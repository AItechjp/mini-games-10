// Ninja AdMax configuration for aitechd.com.
// Paste the admax_id from the asynchronous PC/SP switch ad tag into admaxId.
window.SITE_NINJA_ADMAX = Object.assign({
  enabled: true,
  admaxId: "",
  type: "switch",
  placement: "after-hero"
}, window.SITE_NINJA_ADMAX || {});
